#! /usr/bin/env node

"use strict";

/*
    About line (3) "use strict", this string defines the javascript
    code should be executed in strict mode.

    So, Why Strict mode ?
    -> Strict mode makes it easier to write secure javascript.
    -> mistyping a variable name creates a new variable, in
    strict mode will throw an error, making it impossible to accidentlly create 
    a global variable.

*/

//process.stdin.read()


console.log("Hello world");
console.error("Oops !");